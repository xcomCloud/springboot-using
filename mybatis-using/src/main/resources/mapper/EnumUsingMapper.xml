<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xue.study.mapper.EnumUsingMapper">
    <select id="getGoodsType" resultType="String">
        SELECT DISTINCT(type) FROM `tbl_truck_type_test`;
    </select>
</mapper>